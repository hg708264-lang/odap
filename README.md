# 오답노트 Flutter 앱

문제를 사진으로 촬영하거나 앨범에서 선택하고, PDF 또는 클립보드 텍스트로도 추가할 수 있는 시작 버전입니다.

## 실행

Flutter SDK를 설치한 뒤 프로젝트 루트에서 실행하세요.

```bash
flutter create .
flutter pub get
flutter run
```

`flutter create .`는 생성된 플랫폼 프로젝트 파일을 보완합니다. 이미 작성된 `lib/main.dart`, `pubspec.yaml` 및 권한 설정은 유지하세요.
